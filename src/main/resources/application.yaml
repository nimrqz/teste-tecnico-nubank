spring:
  application:
    name: nubank
  datasource:
    url: jdbc:postgresql://localhost:5432/cadastro
    username: "postgres"
    password: "nicolas9013"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080